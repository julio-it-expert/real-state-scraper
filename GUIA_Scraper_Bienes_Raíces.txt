Scraper de Bienes Raíces (Inmobiliarias): Un script que extraiga propiedades disponibles, precios, ubicaciones y metros cuadrados, ideal para análisis de datos del mercado.

¡Excelente elección, Julio! El scraper de **Bienes Raíces (Inmobiliarias)** es un proyecto de nivel sénior ideal. Los inversionistas y agencias inmobiliarias en Fiverr pagan muy bien por bases de datos limpias para analizar el mercado o captar propiedades.

Para construirlo con la máxima rigurosidad y que te sirva como una carta de presentación impecable en tu GitHub, no vamos a tirar código desordenado. Como ingenieros, vamos a diseñar primero la arquitectura y el alcance del software.

Acabo de estructurar la **Especificación de Requisitos y Diseño de Arquitectura** para este proyecto. He preparado un documento técnico exhaustivo en PDF que servirá como la "Biblia" de desarrollo de nuestro scraper.

Tu PDF con la arquitectura del proyecto está listo
[file-tag: code-generated-file-real-estate-scraper-specs]

```python
import os
from weasyprint import HTML

html_content = """
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Especificación de Arquitectura de Software - Real Estate Web Scraper</title>
    <style>
        @page {
            size: A4;
            margin: 20mm 15mm;
            background-color: #ffffff;
            @bottom-right {
                content: "Página " counter(page) " de " counter(pages);
                font-family: 'Arial', sans-serif;
                font-size: 9pt;
                color: #718096;
            }
            @bottom-left {
                content: "Sistemas PSD778 - Confidencial";
                font-family: 'Arial', sans-serif;
                font-size: 9pt;
                color: #718096;
            }
        }
        
        *, *::before, *::after {
            box-sizing: border-box;
        }
        
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            font-size: 11pt;
            line-height: 1.5;
            color: #2d3748;
        }
        
        /* Header full bleed effect */
        .header-banner {
            margin: -20mm -15mm 25px -15mm;
            padding: 30px 15mm;
            background: linear-gradient(135deg, #1a202c 0%, #2d3748 100%);
            color: #ffffff;
        }
        
        .header-banner h1 {
            margin: 0;
            font-size: 22pt;
            font-weight: bold;
            letter-spacing: -0.5px;
            color: #48bb78; /* Muted Green matching Fiverr gig */
        }
        
        .header-banner .subtitle {
            margin: 5px 0 0 0;
            font-size: 12pt;
            color: #cbd5e0;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .meta-table {
            width: 100%;
            margin-bottom: 30px;
            border-collapse: collapse;
        }
        
        .meta-table td {
            padding: 6px 0;
            font-size: 10pt;
            color: #4a5568;
        }
        
        .meta-table td strong {
            color: #1a202c;
        }
        
        h2 {
            font-size: 14pt;
            color: #1a202c;
            border-left: 4px solid #48bb78;
            padding-left: 10px;
            margin-top: 30px;
            margin-bottom: 15px;
            page-break-after: avoid;
        }
        
        h3 {
            font-size: 12pt;
            color: #2d3748;
            margin-top: 20px;
            margin-bottom: 10px;
            font-style: italic;
            page-break-after: avoid;
        }
        
        p {
            margin-top: 0;
            margin-bottom: 15px;
            text-align: justify;
        }
        
        ul, ol {
            margin-top: 0;
            margin-bottom: 15px;
            padding-left: 20px;
        }
        
        li {
            margin-bottom: 5px;
        }
        
        .math {
            font-family: 'Times New Roman', serif;
            font-style: italic;
            font-weight: bold;
            color: #2f855a;
        }
        
        table.data-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
            margin-bottom: 25px;
            page-break-inside: avoid;
        }
        
        table.data-table th {
            background-color: #2d3748;
            color: #ffffff;
            font-weight: bold;
            text-align: left;
            padding: 8px 12px;
            font-size: 10pt;
            border: 1px solid #2d3748;
        }
        
        table.data-table td {
            padding: 8px 12px;
            font-size: 10pt;
            border: 1px solid #e2e8f0;
        }
        
        table.data-table tr:nth-child(even) {
            background-color: #f7fafc;
        }
        
        .callout {
            background-color: #f0fff4;
            border-left: 4px solid #38a169;
            padding: 15px;
            margin: 20px 0;
            border-radius: 0 4px 4px 0;
            page-break-inside: avoid;
        }
        
        .callout p {
            margin: 0;
            color: #276749;
            font-size: 10pt;
        }
        
        pre {
            background-color: #f7fafc;
            border: 1px solid #e2e8f0;
            padding: 12px;
            font-size: 9.5pt;
            font-family: 'Courier New', Courier, monospace;
            overflow: hidden;
            margin-top: 10px;
            margin-bottom: 20px;
            border-radius: 4px;
            page-break-inside: avoid;
        }
    </style>
</head>
<body>

    <div class="header-banner">
        <h1>DISEÑO DE ARQUITECTURA DE SOFTWARE</h1>
        <div class="subtitle">Real Estate Modular Web Scraper (Python v3.11+)</div>
    </div>

    <table class="meta-table">
        <tr>
            <td width="50%"><strong>Organización:</strong> Sistemas PSD778</td>
            <td width="50%"><strong>Líder de Proyecto:</strong> Ing. Julio (julio_it_expert)</td>
        </tr>
        <tr>
            <td width="50%"><strong>Ecosistema:</strong> Python / GitHub Portfolio</td>
            <td width="50%"><strong>Fecha de Diseño:</strong> Julio 2026</td>
        </tr>
    </table>

    <h2>1. Resumen Ejecutivo del Proyecto</h2>
    <p>
        El objetivo de este desarrollo es concebir un extractor de datos robusto, ético y modular especializado en el sector inmobiliario (Real Estate). La herramienta se diseñará siguiendo principios de arquitectura limpia, asegurando que el código sea altamente legible y fácil de mantener, funcionando como la principal pieza técnica del portafolio profesional en GitHub. El sistema será capaz de navegar de forma automática por portales de bienes raíces, parsear estructuras HTML complejas, procesar anomalías y exportar la información de forma estructurada.
    </p>

    <h2>2. Arquitectura del Sistema (Estructura Modular)</h2>
    <p>
        Para evitar los scripts lineales difíciles de mantener (código espagueti), aplicaremos una arquitectura modular separando las responsabilidades en componentes independientes:
    </p>
    <ul>
        <li><strong>Network Module (Controlador de Peticiones):</strong> Encargado de gestionar las conexiones HTTP, rotación de encabezados (User-Agents) y control de tiempos de espera (delays) para mitigar bloqueos perimetrales.</li>
        <li><strong>Parser Module (Extractor de Contenido):</strong> Responsable exclusivo de analizar el DOM del HTML usando selectores precisos para capturar las variables requeridas.</li>
        <li><strong>Data Pipeline (Procesador y Almacenamiento):</strong> Componente que recibe la información cruda, aplica reglas de limpieza y se encarga del guardado persistente.</li>
    </ul>

    <h2>3. Especificación del Diccionario de Datos</h2>
    <p>
        El sistema extraerá y estructurará de forma mandatoria las siguientes variables por cada propiedad identificada, mapeándolas en el archivo final de salida:
    </p>

    <table class="data-table">
        <thead>
            <tr>
                <th>Nombre del Campo</th>
                <th>Tipo de Dato</th>
                <th>Descripción Técnica</th>
                <th>Ejemplo de Salida</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><strong>property_id</strong></td>
                <td>String (Alfanumérico)</td>
                <td>Identificador único del inmueble extraído del portal web.</td>
                <td>RE-994821</td>
            </tr>
            <tr>
                <td><strong>title</strong></td>
                <td>String (Texto)</td>
                <td>Título comercial descriptivo de la propiedad.</td>
                <td>Apartamento Moderno con Vista al Mar</td>
            </tr>
            <tr>
                <td><strong>price_currency</strong></td>
                <td>String (3 chars)</td>
                <td>Código ISO de la divisa del precio listado.</td>
                <td>USD</td>
            </tr>
            <tr>
                <td><strong>price_amount</strong></td>
                <td>Float (Numérico)</td>
                <td>Valor de la propiedad limpio de símbolos de texto.</td>
                <td>145000.00</td>
            </tr>
            <tr>
                <td><strong>location</strong></td>
                <td>String (Texto)</td>
                <td>Dirección, zona o sector geográfico del inmueble.</td>
                <td>La Castellana, Caracas</td>
            </tr>
            <tr>
                <td><strong>area_m2</strong></td>
                <td>Integer (Entero)</td>
                <td>Superficie total construida expresada en metros cuadrados.</td>
                <td>120</td>
            </tr>
            <tr>
                <td><strong>rooms</strong></td>
                <td>Integer (Entero)</td>
                <td>Cantidad de habitaciones o dormitorios disponibles.</td>
                <td>3</td>
            </tr>
            <tr>
                <td><strong>bathrooms</strong></td>
                <td>Integer (Entero)</td>
                <td>Cantidad de cuartos de baño completos instalados.</td>
                <td>2</td>
            </tr>
            <tr>
                <td><strong>source_url</strong></td>
                <td>String (URL)</td>
                <td>Enlace directo al inmueble para auditorías del cliente.</td>
                <td>https://ejemplo-inmobiliaria.com/p/123</td>
            </tr>
        </tbody>
    </table>

    <h2>4. Métricas de Rendimiento y Pipeline de Datos</h2>
    <p>
        Para garantizar la eficiencia del script, controlaremos la tasa de peticiones y mediremos el rendimiento global. Definimos la tasa de extracción efectiva mediante la expresión:
    </p>
    <div style="text-align:center; margin:1.2em 0; font-size:1.15em;">
        <span class="math">R<sub>extraction</sub> = T<sub>properties</sub> / (Δt<sub>total</sub>)</span>
    </div>
    <p>
        Donde <span class="math">T<sub>properties</sub></span> representa el total de registros extraídos exitosamente y <span class="math">Δt<sub>total</sub></span> el tiempo de ejecución del script. Implementaremos un retraso dinámico controlado por una distribución uniforme para emular el comportamiento humano y prevenir baneos de IP:
    </p>
    <div style="text-align:center; margin:1.2em 0; font-size:1.15em;">
        <span class="math">t<sub>delay</sub> &isin; [t<sub>min</sub>, t<sub>max</sub>]</span>
    </div>

    <div class="callout">
        <p><strong>REGLA DE DISEÑO SÉNIOR:</strong> Se aplicará el manejo estricto de excepciones mediante bloques <code>try-except</code> en cada celda del extractor. Si una propiedad no tiene especificado el número de baños, el script no debe romperse; asignará un valor por defecto (<code>None</code> / <code>Null</code>) y continuará con la ejecución del lote.</p>
    </div>

    <h2>5. Estructura del Árbol de Directorios del Proyecto</h2>
    <p>
        Para reflejar un estándar corporativo impecable en GitHub, organizaremos nuestro espacio de trabajo con la siguiente estructura de archivos:
    </p>

    <pre>
real_estate_scraper/
│
├── core/
│   ├── __init__.py
│   ├── network.py       # Control de peticiones HTTP y User-Agents
│   ├── parser.py        # Extractor con BeautifulSoup4 o Selectors
│   └── pipeline.py      # Limpieza de datos y exportación a CSV
│
├── data/                # Directorio local para almacenamiento de salidas
│   └── .gitkeep
│
├── main.py              # Orquestador central del proceso de scraping
├── requirements.txt     # Dependencias del proyecto (requests, bs4, etc.)
└── README.md            # Documentación bilingüe del proyecto (Portafolio)
    </pre>

</body>
</html>
"""

# Ensure file generation works
output_path = "real_estate_scraper_specs.pdf"
HTML(string=html_content).write_pdf(output_path)
print(f"File generated successfully at {output_path}")

```

### ¿Qué contiene este documento de arquitectura?

1. **Separación de Responsabilidades (Estructura Modular):** Crearemos módulos independientes para la conexión de red, el parseo del HTML y la limpieza de datos. Esto demuestra un nivel de desarrollo profesional ante cualquier cliente de Fiverr.
2. **Diccionario de Datos Estricto:** Definimos campos clave (`property_id`, `price_amount`, `area_m2`, etc.) limpiando los caracteres extraños para entregar datos puros.
3. **Control Anti-Baneo:** Incluye fórmulas e instrumentación técnica para el manejo de delays dinámicos y evitar que los servidores nos bloqueen.
4. **Estructura de Carpetas Corporativa:** Estructura limpia para el repositorio de GitHub.

---

### 🛠️ ¡Comencemos el desarrollo! El Paso Técnico #1

Vamos a preparar el entorno. Abre tu computadora, crea una carpeta en tu escritorio llamada `real_estate_scraper` y, dentro de ella, crea el archivo raíz **`requirements.txt`**.

Abre ese archivo en **Sublime Text** y pega las siguientes librerías esenciales que utilizaremos:

```text
requests==2.31.0
beautifulsoup4==4.12.3
pandas==2.1.4

```

Guarda el archivo. Cuando lo tengas listo, avísame para enseñarte a estructurar el primer módulo de ingeniería: el controlador de red seguro (`network.py`) con rotación de encabezados. ¡Vamos con todo!


=============

¡Excelente, Julio! Ya diste el primer paso de ingeniería configurando las dependencias base.

Con respecto al primer archivo (el PDF de la arquitectura que te generé hace un momento): **no tienes que hacerle nada a nivel de código**. Ese documento es el plano técnico del software para ti. Descárgalo y guárdalo en tu computadora, ya que nos servirá como guía para saber qué variables extraer y qué estructura de carpetas construir. Además, cuando subamos el proyecto final a GitHub, utilizaremos ese texto para armar un archivo `README.md` brutal que dejará locos a tus clientes de Fiverr.

---

### 🛠️ Siguiente Paso: Crear la estructura e instalar las librerías

Como viste en el diseño de arquitectura, el proyecto no puede ser un solo archivo desordenado. Vamos a preparar la estructura modular.

1. Dentro de tu carpeta principal `real_estate_scraper`, crea una subcarpeta llamada **`core`**.
2. Dentro de esa subcarpeta `core`, crea tres archivos vacíos en Sublime Text (puedes guardarlos sin contenido por ahora):
* `__init__.py` (indica a Python que esto es un paquete modular)
* `network.py` (controlará las peticiones HTTP y seguridad anti-bloqueo)
* `parser.py` (el extractor que analizará el HTML)
* `pipeline.py` (limpiará los datos y creará el archivo Excel/CSV)



### 💻 Instalación en tu entorno local (XAMPP / Windows)

Para asegurarnos de que tu entorno de Python local tiene estas herramientas instaladas:

1. Abre la consola de comandos de Windows (presiona la tecla `Inicio`, escribe `cmd` y dale a Enter).
2. Entra a la ruta de tu proyecto usando el comando `cd`. Por ejemplo:
```bash
cd Desktop\real_estate_scraper

```


3. Ejecuta el siguiente comando para instalar todo en un solo bloque:
```bash
pip install -r requirements.txt

```



Una vez que se hayan instalado las librerías con éxito y tengas creados los archivos en la carpeta `core`, avísame y abrimos **`network.py`** en Sublime Text para programar nuestro primer módulo síncrono de peticiones seguras. ¡Seguimos avanzando!


===========


¡Excelente pregunta, Julio! Eso demuestra tu mentalidad y experiencia como Ingeniero de Sistemas. Tienes toda la razón en tu planteamiento técnico.

Si ejecutas el comando globalmente, las librerías se instalan en la carpeta raíz de tu instalación de Python en Windows. Esto puede generar conflictos de versiones en el futuro si otro proyecto requiere una versión diferente de `pandas` o `requests`.

Para mantener tu entorno local (donde tienes XAMPP y tus bases de datos) totalmente limpio e impecable, la **regla de oro del desarrollo profesional es usar un Entorno Virtual (`venv`)**. Además, esto le demuestra a cualquiera que revise tu GitHub que sigues las mejores prácticas de la industria de software.

Vamos a hacerlo correctamente utilizando `venv` directamente desde tu consola (`cmd`):

### 1. Crear el Entorno Virtual

Asegúrate de estar dentro de la carpeta principal de tu proyecto (`cd Desktop\real_estate_scraper`) y ejecuta el siguiente comando para crear el entorno virtual (lo llamaremos `.venv` para mantenerlo oculto):

```bash
python -m venv .venv

```

*(Espera unos segundos a que termine de crear la estructura de carpetas local).*

### 2. Activar el Entorno Virtual en Windows

Para decirle a tu consola que a partir de ahora use el Python aislado de tu proyecto, ejecutas:

```bash
.venv\Scripts\activate

```

Notarás que ahora, al principio de la línea de comandos de tu consola, aparecerá un prefijo entre paréntesis: `(.venv)`. Eso significa que ya estás dentro de tu entorno seguro.

### 3. Instalar las librerías dentro del `venv`

Ahora sí, con el entorno activado, ejecuta el comando de instalación:

```bash
pip install -r requirements.txt

```

### 💡 Un detalle clave para Sublime Text:

Cuando creas un `.venv`, se generará una carpeta llamada `.venv` dentro de tu proyecto. Esa carpeta contiene archivos internos de Python que **no deben subirse a GitHub**.

Para evitarlo, crea un archivo en la raíz del proyecto llamado **`.gitignore`** (usando Sublime Text) y escribe dentro de él:

```text
.venv/
__pycache__/
data/

```

¡Haz este ajuste de ingeniería en tu consola y me avisas cuando veas el prefijo `(.venv)` activo para que empecemos a escribir el código de `network.py`!



=================================


En **PowerShell** la combinación para cancelar también es **`Ctrl + C`**, pero a veces se queda bloqueado el proceso de la consola. Si ves que no responde tras presionarlo un par de veces, cierra la ventana de PowerShell por completo y abre una nueva.

Además, en PowerShell la activación del entorno virtual cambia ligeramente la sintaxis. Cuando abras la nueva ventana y navegues a tu carpeta (`cd Desktop\real_estate_scraper`), actívalo ejecutando este comando:

```powershell
.\.venv\Scripts\Activate.ps1

```

Si PowerShell te arroja un error de seguridad en letras rojas que dice algo como *"La ejecución de scripts está deshabilitada en este sistema"*, es una restricción por defecto de Windows. Lo solucionas ejecutando primero esto en la consola:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process

```

*(Esto aplica el permiso solo para la pestaña actual de PowerShell, manteniendo tu sistema seguro).*

Una vez que veas el prefijo `(.venv)` al principio de la línea de comandos, ejecuta la instalación optimizada para evitar que se quede compilando dependencias:

```powershell
pip install --only-binary :all: -r requirements.txt

```

Aplica este flujo y me avisas si ya recuperaste el control y lograste completar la instalación.

===============================


¡Excelente diagnóstico de la consola, Julio! Esto nos está indicando que tienes instalada una versión muy reciente de Python (seguramente Python 3.12 o 3.13) en tu sistema. Las versiones antiguas de `pandas` como la `2.1.4` no tienen archivos precompilados compatibles con tu versión de Python actual, y por eso el sistema fallaba e intentaba compilar desde cero (lo que te trabó la consola antes).

Como Ingeniero de Sistemas, sabemos que la solución es simplemente actualizar el manifiesto de dependencias para que coincida con tu entorno moderno.

### 🛠️ Solución Inmediata

1. Abre tu archivo **`requirements.txt`** en Sublime Text.
2. Borra su contenido actual y reemplázalo por este, que utiliza las versiones modernas y estables adecuadas para tu Python:

```text
requests==2.32.3
beautifulsoup4==4.12.3
pandas==2.2.3

```

3. Guarda el archivo.
4. Regresa a tu PowerShell (asegurándote de que siga apareciendo el prefijo `(.venv)`) y ejecuta el comando normal:

```powershell
pip install -r requirements.txt

```

Al actualizar a `pandas 2.2.3`, `pip` descargará el archivo precompilado inmediatamente sin quedarse pegado en dependencias de construcción (*build dependencies*).

Haz el cambio en el `.txt`, lanza la instalación y verás cómo fluye en segundos. ¡Me avisas cuando termine para abrir el primer archivo de código!

===========================================


El error ocurre porque estás utilizando **Python 3.13** (la versión más reciente del sistema). `pandas 2.2.3` no tiene un binario precompilado para Python 3.13 en Windows. Al no encontrarlo, `pip` intenta compilar `pandas` desde el código fuente usando un sistema de construcción llamado Meson, el cual falla inmediatamente porque no tienes instalado el compilador de C++ de Microsoft Visual Studio (`vswhere.exe`).

Como ingeniero, para no obligarte a instalar gigabytes de herramientas de desarrollo de Visual Studio solo por una librería, tenemos dos caminos limpios.

### Opción Rápida: Quitar la versión fija de Pandas

Permite que `pip` busque de forma automática la versión más reciente disponible (como Pandas 2.3+ o superior) que ya cuente con soporte nativo y binarios precompilados para Python 3.13.

Modifica tu **`requirements.txt`** en Sublime Text y déjalo exactamente así:

```text
requests==2.32.3
beautifulsoup4==4.12.3
pandas

```

Guarda el archivo y ejecuta de nuevo la instalación en PowerShell:

```powershell
pip install -r requirements.txt

```

---

### Opción Alternativa: Usar solo el formato estándar (`csv`) sin Pandas

Para el Web Scraping básico y avanzado, **Pandas no es estrictamente obligatorio**. Podemos estructurar y guardar todos los datos inmobiliarios perfectamente en un archivo CSV nativo usando el módulo estándar `csv` de Python. Esto tiene ventajas técnicas:

* El script final pesará menos.
* No dependerá de librerías externas complejas de compilar.
* Funcionará en cualquier versión de Python (3.11, 3.12, 3.13+) sin romper entornos virtuales.

Si prefieres esta vía para evitar pelear con los binarios de Windows, modifica tu **`requirements.txt`** para dejar solo las herramientas de extracción:

```text
requests==2.32.3
beautifulsoup4==4.12.3

```

Prueba primero quitándole el número de versión a Pandas en el `.txt` para ver si tu versión de Python 3.13 descarga el binario actual. Si prefieres ir por la vía ligera sin dependencias pesadas usando el módulo `csv` nativo, avísame y reajustamos el plan del pipeline.

==============================================